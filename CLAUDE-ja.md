# Claude Code ガイドライン（日本語ミラー）

> **このファイルは人間用のミラーです。** Claude は英語正本 (`CLAUDE.md`) を読みます。  
> このファイルにはレビュー・改善に役立つ rationale（なぜそう決めたか）を追記しています。

---

## リポジトリの正体（重要）

- `~/.claude` はこのリポジトリ（`/Users/yamashitashoutarou/Sources/github.com/yama-sitter/claude-config`）へのシンボリックリンクです。`~/.claude/` 配下のファイル（`settings.json` / `CLAUDE.md` / `rules/` / `hooks/` / `skills/`）はすべてこのリポジトリで Git 管理されています。`ls -la ~/.claude` で確認できます。
- Edit が Self-Modification guard 等でブロックされた場合も、「編集不可」と結論づけないでください。正規ルートは「リポジトリ側のファイルを編集 → `git commit` → 新セッションで再ロード」です。guard はファイル名だけでなく内容でもマッチする場合があるため、リポジトリ側パスでの編集もブロックされることがあります。

---

## 全般

- 明確化のための質問はユーザーの意図が本当に曖昧な場合のみ行う。ユーザーがすでに明示した要件については、サブエージェントや計画が別のアプローチを推奨していても再確認しない。
- 正確で誠実な情報のみを提供する。お世辞や sycophancy は禁止。
- YAGNI：今必要でないものを作らない。「後で役に立つかもしれない」は作る理由にならない。
- 迷ったらシンプルな選択肢を取る——行数が少なく、可動部分が少ないもの。
- 既存のコードベースのパターンに従う。類似の実装があれば、それに合わせる。
- 新たな抽象、レイヤー、ユーティリティを導入しない。必要な場合は計画書に理由を記してユーザーの承認を得る。
- 変更はリクエストされたスコープ内に留める。周辺の改善やリファクタリングを含めない。
- 応答は簡潔に——結果と判断を直接述べる。既に確立した内容の繰り返しや、日常的な操作の逐一な実況は避ける。
- subagent に委譲する際は、各エージェントに一度だけ的確に指示し、報告後にその内容を再度自分で導出し直さない。並列 subagent 数はタスクが明確に必要とする場合を除き少数（目安 5〜8）に抑える。
- **常に日本語で応答する**——会話テキストのみ対象。ファイル編集には適用しない。
- **ファイルを編集する際は常にそのファイルの既存言語に合わせる**——会話言語をファイル言語に持ち込まない。

> **Rationale**: "Always respond in Japanese" は会話の可読性と確認のしやすさのため。ファイル言語の維持は二言語管理ポリシーとの整合性のため（英語正本を日本語で上書きしてしまうリスクを防ぐ）。

---

## 二言語ドキュメント管理

英語正本 + 日本語ミラーのポリシーは `rules/skill-bilingual.md` を参照してください。

> **Rationale**: Claude にとって英語はトークン効率が良く、命令形の規約文体も書きやすい。人間は日本語ミラーで素早く内容を把握できる。このため非対称（英語正本 + 日本語ミラー）という設計を採用した。

---

## Plan Mode のルール

- 計画書を書く際は `plan-template.md` で定義されたプラン構造に従う（superpowers フローが有効な場合を除く）。
- ユーザーが計画を承認（ExitPlanMode で承認）したら、Memory Guidelines に従って計画を保存する。

---

## スキル競合の解決（superpowers vs カスタムスキル）

### superpowers フローが有効な場合

以下のいずれかが true のとき、superpowers フローは有効とみなす：

- ユーザーが明示的に `/superpowers:brainstorming` を呼び出した
- ユーザーが明示的に superpowers の使用を要求した（例：「superpowers を使って」「superpowers フローで」）
- 実装が `docs/superpowers/plans/` にある計画ファイルによって進められている

有効な場合：

- 計画テンプレート → `superpowers:writing-plans` を使用（`plan-template.md` は使わない）
- 計画レビュー → superpowers サブエージェントレビューを使用
- 計画の保存 → `docs/superpowers/plans/` に保存 → Execution Handoff の前に Memory Guidelines の手順で `agent-memory` にも保存

### superpowers フローが有効でない場合

直接のコード変更リクエスト、Plan Mode、またはユーザーが明示的に superpowers を要求していないタスクはこちら。

- Worktree → カスタム `worktree` スキル（EnterWorktree/ExitWorktree）を使用
- ブレインストーミング・設計 → カスタム `brainstorm` スキルを使用
- 計画テンプレート → `plan-template.md` を使用
- 計画の保存 → `agent-memory` のみ

### 常に適用（フローに関係なく）

- フローに関係なく、`rules/` 配下のすべてのガイドラインを適用する。
- `superpowers:brainstorming` が自動トリガーされるとしても、ユーザーが明示的に superpowers を要求しない限りカスタムスキルを優先する。
- Worktree 操作は常にカスタム `worktree` スキル（EnterWorktree/ExitWorktree）を使用する。フローに関係なく。
- ブレインストーミング・設計は常にカスタム `brainstorm` スキルを使用する。ユーザーが明示的に superpowers を要求した場合を除く。

> **Rationale**: superpowers はサードパーティの計画 UI・レビュー UI を提供するが、worktree 操作とブレインストーミングはカスタムスキルの方が精度が高い。フロー混在を防ぐため明示的な判定条件を設けた。

---

## セッション管理

- 1セッション1タスク。現在のタスク完了後は `/clear` で次のタスクを開始する。
- タスクを切り替える前に、重要な決定事項をメモリに保存する（`/agent-memory`）。
- 計画と実装が同一セッションで行われる場合、フェーズ間で `/compact` を実行する。
- Plan Mode（計画）と実装は別セッションで行うことを推奨する。計画ファイルがギャップを埋める。

> **Rationale**: コンテキストウィンドウの枯渇を防ぎ、計画段階と実装段階の責任分離を明確にするため。「1セッション1タスク」はトークン効率と振り返りやすさのルール。

---

## Compact 時の指示

コンパクト時に保存する情報：

- ユーザーのリクエストと意図
- 変更したファイルパスと変更内容のサマリー
- 未完了のタスクと現在の状態
- 重要な技術的決定とその根拠
- 発生したエラーとその解決方法
